# Git Forest

ASCII-art git graph for the terminal.

## Usage
```
cargo build --release
./target/release/git-forest --repo /path/to/repo
```

## How it works
Renders commit history as branching ASCII trees. Arrow keys to scroll, click to inspect commits. Export to SVG supported.
